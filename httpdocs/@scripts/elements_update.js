$(document).ready(function () {

    // var script = document.createElement('script');
    // script.src = "elements.ashx?v=" + services.getItemLocal("ElementsVersion");
    // document.head.append(script);

    // script.onload = function () {
    //     //var old_elements = services.getItemLocal("ElementsArray") || [];
    //     //var old_version = services.getItemLocal("ElementsVersion") || 0;
    //     services.setItemLocal("ElementsArray", elements);
    //     services.setItemLocal("ElementsVersion", version);

    //     for (var i = 0; i < elements.length; i++) {
    //         var element = elements[i];

    //         if (element.type == "html") {
    //             services.registerTemplate(element.name, element.content);
    //         }

    //         if (element.type == "js") {
    //             var script = "var file = { name:'" + element.name + "', path:'" + element.path + "' }; ";
    //             eval(script + element.content);
    //         }
    //     }

    // };

});